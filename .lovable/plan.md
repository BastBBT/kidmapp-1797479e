Remplacer l'icône « Jeux » par le nouveau PNG uploadé (`user-uploads://jeu-2.png`), avec le même recadrage bbox + marge 6% que les 4 autres activités, puis remplacer le pointeur `src/assets/cat-jeux.png.asset.json`. Aucun changement de code.