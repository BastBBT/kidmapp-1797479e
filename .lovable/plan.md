## Correction

Dans `src/pages/Index.tsx`, supprimer la ligne `<MealFilter />` dupliquée (l'une des deux lignes 153/154 identiques). Une seule occurrence doit rester à l'intérieur du conteneur animé `showMealFilter`.

Aucun autre changement.